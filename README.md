# 诗歌网站

这是一个基于 Zola 静态网站生成器构建的诗歌网站，收录了诗人李瑜的完整诗集。

**生产环境**: https://www.liyupoetry.com  
**GitHub Pages**: https://lukethecat.github.io  
**GitHub 仓库**: https://github.com/lukethecat/lukethecat.github.io

---

## ⚠️ 重要提示：隐私保护

**严禁将以下信息提交到 GitHub：**
- ❌ 个人邮箱地址（使用 `@` 等占位符代替）
- ❌ 真实姓名（使用“李瑜”作为公开名称，避免化学名）
- ❌ 电话号码、身份证号、地址等个人信息
- ❌ 本地文件路径（如 `/Users/jellyfishjaco/...`）
- ❌ 私有 API Key、Token、密码
- ❌ 任何包含个人身份信息的文件

**正确的做法：**
- ✅ 使用 `[REDACTED]` 或 `@username` 替换敏感信息
- ✅ 在 `.gitignore` 中添加敏感文件模式
- ✅ 使用 GitHub Secrets 存储 API Key（不直接写在代码中）
- ✅ 确保 `config.toml` 中的 `base_url` 仅包含域名

---

## 📋 发布前检查清单 (每次 push 前必做)

**发布前必须按顺序执行以下操作：**

### 1. ✅ 代码清理检查
```bash
# 1. 检查 Git 状态
git status --short

# 2. 查看有无被跟踪的构建输出文件
git ls-files | grep -E "(^public/|\.pyc$|__pycache__)" | head -5

# 3. 确认 .gitignore 包含 public/
grep "^public/" .gitignore || echo "警告: public/ 未在 .gitignore 中"
```

**检查项：**
- [ ] 无无意识的文件修改（`\?` 符号）
- [ ] `public/` 目录不在 Git 跟踪中
- [ ] `.gitignore` 包含 `public/`
- [ ] 无临时文件、缓存文件提交

### 2. ✅ 隐私保护检查
```bash
# 检查代码中是否包含个人信息
grep -r "jellyfishjaco" --include="*.md" --include="*.toml" --include="*.py" .
grep -r "\.com/Documents" --include="*.md" --include="*.toml" --include="*.py" .
grep -r "[DETECTED]" --include="*.md" --include="*.toml" --include="*.py" .
```

**检查项：**
- [ ] 无本地用户目录（如 `/Users/username/`）
- [ ] 无真实邮箱地址（仅使用 `@username` 或 `@example.com`）
- [ ] 无 API Key 等密钥
- [ ] 无电话号码、身份证号

### 3. ✅ Zola 构建测试
```bash
# 清理并重新构建
rm -rf public/
zola build

# 检查构建结果
ls -la public/
echo "页面数量: $(find public/ -name "*.html" | wc -l)"
```

**检查项：**
- [ ] 构建成功（无错误）
- [ ] 生成了 `public/` 目录
- [ ] 至少有 100 个 HTML 文件（诗歌页面）
- [ ] 主页 `public/index.html` 存在

### 4. ✅ 本地预览测试（可选但推荐）
```
zola serve
# 访问: http://127.0.0.1:11000
```

**检查项：**
- [ ] 网站能够正常访问
- [ ] 诗歌列表正确显示
- [ ] 链接有效无 404

### 5. ✅ Git 提交信息规范
提交信息必须采用以下格式：
```
<类型>(<范围>): <简洁描述>

<可选的详细描述>

<变更原因>
<相关问题/Issue>
```

**提交类型：**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/辅助工具变更
- `ci`: CI/CD 配置变更

**示例：**
```bash
git add .
git commit -m "fix(ci): update branch from master to main

- 更新 GitHub Actions 工作流配置
- 修复 Cloudflare Pages 部署触发问题
- 确保 push 到 main 分支时正确部署"
```

### 6. ✅ 提交前最终检查
```bash
# 查看即将提交的内容
git diff --cached

# 确认无 public/ 目录文件
git diff --cached --name-only | grep "public/" && echo "错误：包含 public/ 文件" || echo "通过"

# 确认无敏感信息
git diff --cached | grep -E "(password|token|key|@|12345|个人)" && echo "警告：可能包含敏感信息" || echo "通过"
```

---

## 🚀 部署流程

### 自动化部署（推荐）

1. **提交代码**
   ```bash
   # 完成发布前检查清单后
   git add .
   git commit -m "feat: <你的修改描述>"
   git push origin main
   ```

2. **GitHub Actions 自动触发**
   - 代码推送到 `main` 分支触发 CI/CD
   - 执行 `make build` 构建静态网站
   - 自动部署到 Cloudflare Pages

3. **监控部署状态**
   - GitHub Actions: https://github.com/lukethecat/lukethecat.github.io/actions
   - Cloudflare Pages Dashboard: 登录 Cloudflare 账号查看

4. **验证部署**
   - 等待 1-5 分钟
   - 访问生产环境网站
   - 检查更新内容

### 部署后验证检查表 (每次 push 后必做)

**部署后必须按顺序验证以下项目：**

#### 1. ✅ GitHub Actions 部署状态验证
- [ ] 访问 GitHub Actions: https://github.com/lukethecat/lukethecat.github.io/actions
- [ ] 确认最新 workflow 运行状态为 ✅ 绿色 (Success)
- [ ] 查看 Build 日志，确认无错误信息
- [ ] 确认 deploy 步骤执行完成

#### 2. ✅ Cloudflare Pages 部署状态验证
- [ ] 登录 Cloudflare Dashboard
- [ ] 进入 Pages > liyupoetry 项目
- [ ] 确认最新部署状态为 ✅ Success
- [ ] 查看部署日志，确认所有文件已上传
- [ ] 检查部署的 URL (如: https://<hash>.liyupoetry.pages.dev)
- [ ] 访问该 URL 测试预发布环境

#### 3. ✅ 生产环境网站验证

**关键页面检查** (每个页面必须返回 200 状态码):

```bash
# 使用 curl 或浏览器访问以下 URL:
```

- [ ] **首页**: https://www.liyupoetry.com/
  - 预期: 200 OK, 显示诗人介绍和书籍列表
  
- [ ] **汗血马诗集** (1995hanxuema) - 修复的 404 页面:
  - [ ] https://www.liyupoetry.com/1995hanxuema/
  - 预期: 200 OK, 显示诗集目录
  - [ ] https://www.liyupoetry.com/1995hanxuema/汗血马/
  - 预期: 200 OK, 显示具体章节
  
- [ ] **1980 诗集**:
  - [ ] https://www.liyupoetry.com/1980/
  - 预期: 200 OK, 显示诗集内容
  
- [ ] **归档页面**:
  - [ ] https://www.liyupoetry.com/archive/
  - 预期: 200 OK, 显示所有书籍列表
  
- [ ] **关于页面**:
  - [ ] https://www.liyupoetry.com/about/
  - 预期: 200 OK

**验证命令示例**:
```bash
# 检查状态码
curl -s -o /dev/null -w "%{http_code}" https://www.liyupoetry.com/1995hanxuema/

# 或批量检查
pages=(
  "https://www.liyupoetry.com/"
  "https://www.liyupoetry.com/1995hanxuema/"
  "https://www.liyupoetry.com/1980/"
  "https://www.liyupoetry.com/archive/"
)

for url in "${pages[@]}"; do
  status=$(curl -s -o /dev/null -w "%{http_code}" "$url")
  echo "$url: $status"
done
```

#### 4. ✅ 内容验证

- [ ] 首页显示最新的诗歌集合
- [ ] 所有书籍封面图片加载正常
- [ ] 诗歌列表完整 (101 首诗歌)
- [ ] 导航链接有效无 404
- [ ] 移动端显示正常 (响应式设计)

#### 5. ✅ 功能验证

- [ ] 搜索功能正常工作
- [ ] 分类和标签页面可访问
- [ ] 深色/浅色主题切换正常
- [ ] RSS/Atom 订阅源可访问 (https://www.liyupoetry.com/atom.xml)

#### 6. ✅ 错误页面验证

- [ ] 404 页面样式正确: https://www.liyupoetry.com/nonexistent-page/
- [ ] 返回正确的 404 状态码

---

**⚠️ 如果发现问题**:

1. **检查部署日志**: 查看 GitHub Actions 和 Cloudflare Pages 的详细日志
2. **清除缓存**:
   - Cloudflare: 清除 CDN 缓存 (Caching > Configuration > Purge Everything)
   - 浏览器: 清除本地缓存或硬刷新 (Ctrl+Shift+R 或 Cmd+Shift+R)
3. **等待 5-10 分钟**: CDN 全球传播需要时间
4. **检查 DNS**: 使用工具如 `dig www.liyupoetry.com` 或 `nslookup www.liyupoetry.com`
5. **回滚**: 如有严重问题，回滚到上一个稳定版本的 Git commit

---

**自动化验证 (CI/CD)**:

GitHub Actions 工作流已自动包含部署验证步骤:
- 构建后自动检查关键页面状态码
- 部署失败会收到通知
- 查看 `.github/workflows/ci.yml` 中的 `Validate deployment` 步骤

---

### 手动部署（仅用于测试）

```bash
# 1. 检查版本
zola --version

# 2. 清理旧构建
rm -rf public/

# 3. 构建
zola build

# 4. 部署（Cloudflare Pages 会自动从 GitHub 拉取）
# 或者使用 Cloudflare CLI
```

---

## 技术栈

| 组件 | 说明 |
|------|------|
| **Zola** | 静态网站生成器，使用 Rust 编写 |
| **GitHub Actions** | 自动化构建与部署 |
| **Cloudflare Pages** | 生产环境部署 |
| **TinySearch** | 浏览器端诗歌搜索索引 |
| **Make** | 构建自动化脚本 |
| **ImageMagick** | 图片尺寸调整与优化 |

---

## 项目结构

```
liyupoerty.com-master/
├── .github/
│   └── workflows/
│       └── ci.yml          # GitHub Actions CI/CD 配置 ❗️使用 main 分支
├── .gitignore             # Git 忽略文件配置（必须包含 public/）
├── config.toml            # Zola 全局配置
├── Makefile               # 构建脚本
├── README.md              # 项目说明（本文件）
├── website-maintenance-manual.md  # LLM/开发者维护指南
├── content/               # 网站内容（Markdown 源文件）
│   ├── _index.md         # 首页内容
│   ├── static/           # 静态页面 (about, archive)
│   ├── 1980/             # 1980 诗集
│   └── 1995hanxuema/     # 汗血马诗集（核心）
│       ├── _index.md     # 诗集元数据
│       ├── 汗血马 李瑜 QWEN校对20260203.md  # 内容源文件
│       └── [21个章节...]  # 101首诗歌
├── templates/             # Zola 页面模板
│   ├── index.html        # 首页模板
│   ├── section.html      # 目录/章节模板
│   ├── page.html         # 内容页面模板
│   └── ...               # 其他模板
├── static/               # 静态资源（CSS、JS、图片）
│   ├── css/              # 样式表
│   ├── fonts/            # 字体文件
│   ├── images/           # 图片
│   │   └── covers/      # 书籍封面
│   └── js/               # JavaScript 文件
├── helpers/              # 构建辅助脚本
└── generate_poetry_pages.py  # 自动生成诗歌页面脚本
```

**⚠️ 重要提示：**
- `public/` 目录**不**应该被 Git 跟踪（已在 `.gitignore` 中）
- `public/` 目录由 Zola 构建时自动生成
- Cloudflare Pages 使用 GitHub Actions 运行 `make build` 生成 `public/`

---

## 🔧 本地开发

### 安装依赖

```bash
# 安装 Zola (Mac)
brew install zola

# 验证安装
zola --version
```

### 启动开发服务器

```bash
# 进入项目根目录
cd /path/to/liyupoerty.com-master

# 启动热重载开发服务器
zola serve

# 访问: http://127.0.0.1:11000
```

### 构建生产版本

```bash
# 方法1: 使用 Make 命令
make clean
make build

# 方法2: 直接使用 Zola
rm -rf public/
zola build --base-url https://liyupoerty.com
```

---

## 🔍 故障排查

| 问题 | 解决方案 |
|------|----------|
| **构建失败** | 检查 `content/` 下所有 `_index.md` 文件的前端元数据格式 |
| **404 页面** | 确保章节文件夹命名与 `config.toml` 中路径匹配 |
| **搜索功能失效** | 重新运行 `make search` 或检查 Wasm 文件 |
| **图片不显示** | 检查 `static/images/covers/` 目录结构 |
| **Cloudflare 未更新** | 1. 检查 GitHub Actions 状态<br>2. 验证分支名是 `main` 不是 `master`<br>3. 检查 `.gitignore` 是否正确 |
| **页面显示旧内容** | 1. 清除浏览器缓存<br>2. 等待 CDN 缓存刷新（最长 5 分钟）<br>3. 检查 Cloudflare 舞台仪表板 |

**高级调试:**
```bash
# 查看构建日志
zola build --verbose

# 检查 Git 状态
git status
git log --oneline -5

# 验证 Git 历史（检查是否有提交 public/ 的错误）
git log --stat --oneline | grep "public/" | head -5
```

---

## 📝 发布日志模板

每次推送前，请更新此部分：

### 未发布 (Unreleased)

**变更类型:**
- [ ] 新功能 (feat)
- [ ] Bug 修复 (fix)
- [ ] 文档更新 (docs)
- [ ] 性能优化 (perf)
- [ ] 其他 (chore)

**变更内容:**
- 
**相关问题:**
- 

**发布日期:**
- 2026-XX-XX

---

## 🤖 CI/CD 配置

### GitHub Actions 分支配置

**已修正:** `.github/workflows/ci.yml` 配置使用 `main` 分支触发部署：

```yaml
on:
  push:
    branches: [main]  # ✅ 正确配置
  pull_request:
    branches: [main]  # ✅ 正确配置
```

### 部署触发条件

1. **代码推送** - 推送到 `main` 分支
2. **计划任务** - 每月 1 日 18:00 UTC
3. **手动触发** - 通过 GitHub Actions 界面手动运行

### 构建步骤

GitHub Actions 执行顺序：
1. 安装 Zola、gh-stats、tinysearch、cavif、wasm-opt、terser
2. 生成 GitHub 星球统计 (`make stars`)
3. 构建图片 (`make images`)
4. 构建网站内容 (`zola build`)
5. 生成搜索索引 (`make index`)
6. 压缩 JavaScript (`make minify`)
7. 部署到 Cloudflare Pages

---

## 🙏 赞助商与鸣谢

- **Cloudflare Pages**: 可靠的静态网站托管
- **GitHub Actions**: 自动化 CI/CD
- **Zola 社区**: 优秀的静态网站生成器

---

## 📞 问题与反馈

如有问题，请：
1. 检查本 README 文档
2. 查阅 `website-maintenance-manual.md` 获取详细维护指南
3. 在 GitHub Issues 中提交问题（注意：避免私有信息暴露）
4. 联系开发者前确保已完成发布前检查清单

---

## 许可证

© 李瑜 版权所有  
本诗歌作品为原创，未经作者明确书面许可，不得擅自转载、复制、改编或用于任何商业用途。

**免责声明:** 本项目为开源静态网站项目，所有代码和配置在 GitHub 上公开。**请勿提交任何个人隐私信息到仓库中。**

---

## 📋 检查清单标准化模板

### 新功能发布模板
```markdown
## 🚀 新功能发布准备

### 代码更改
- [ ] 功能代码编写完成
- [ ] 通过本地测试
- [ ] 代码符合项目规范

### 文档更新
- [ ] README.md 已更新
- [ ] 相关文档已更新
- [ ] 提交信息规范

### 发布前验证
- [ ] 完成发布前检查清单（1-6）
- [ ] 无敏感信息泄露
- [ ] 构建测试通过
- [ ] 本地预览正常

### 部署后验证
- [ ] GitHub Actions 部署成功
- [ ] Cloudflare Pages 更新完成
- [ ] 生产环境网站可访问
- [ ] 功能正常工作

### 定期维护
- [ ] 每周检查一次 GitHub Actions 日志
- [ ] 每月更新文档
- [ ] 每季度清理 Git 历史（如需要）
```

---

**版本**: 1.1 (2026-02-03)  
**最后更新**: 2026-02-04  
**维护者**: [开发者团队]

⚠️ **请务必在每次修改后阅读并更新本 README**